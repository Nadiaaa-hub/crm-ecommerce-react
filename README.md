How to run the project

- `server` — backend part
- `project-shop` — frontend part

1. Run the server
Open the first terminal and go to the `server` folder:

cd server,
npm install,
npm start

2. Run the frontend
Open a second terminal and go to the `project-shop` folder:

cd project-shop,
npm install,
npm run dev
